{
  "description": "Auto-approve read-only OpenDeploy CLI checks and npm metadata reads",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/auto-approve-cli.sh"
          }
        ]
      }
    ]
  }
}
